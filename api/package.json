{
  "name": "rbi-timeline-api",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Express read API over the RBI clause timeline (Postgres). Python fills the DB; this only reads.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
